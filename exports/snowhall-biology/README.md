# Snowhall 生物製作證據

2026-09-13，使用者確認銀魚單一主角版本。

- 合併前驗證：`release-tests.log`（143 項）、`release-build.log`；另補相機 target-only 更新回歸測試。
- 最終視覺候選：`silverfish-only-*.png`、`silverfish-focus-browser.json`、`silverfish-focus-full-tests.log`、`silverfish-focus-build.log`、`qa.json`。QA 是 10 seeds × 600 秒加速數值模擬，不是實機錄影。
- 模型留存：`squid-model.png`、`silverfish-model.png`；模型來源在 `src/shared/biology/antarctic/AntarcticModels.ts`。
- 歷史：`baseline.png` 為原魟魚；`noon-transit.png`／`moonlight-transit.png`／`phone-noon.png` 為被否決的橫向方案；`room-route-*.png` 是第一版前後往返；`focus-*.png` 是未採用的低處近景魷魚候選。
- 其餘 log／JSON 保留製作過程，可能含中途失敗，不能當作最終成功證據。不同 elapsed／畫幅的截圖不可宣稱像素 A/B。
- 最新決策及測試條件以 `docs/scenes/snowhall.md` 為準。
