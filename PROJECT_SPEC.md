# Quiet Places／靜隅：集合產品規格

> 可以進去待一會的靜謐空間集合。

文件狀態：**集合部分實作**。目前可切換水光之間、樹影午後與海光之室；集合首頁與下述 hash 路由仍為規劃。現行功能與截圖見 README。

## 1. 品牌與邊界

- 集合品牌：Quiet Places／靜隅。套件與 repository 使用 `quiet-places`；日常裡可以安靜停留的角落是共同主題。
- 場景名稱獨立於集合品牌：水光之間／Waterlight，固定 ID `waterlight`；其餘場景依各自內容命名。
- 定位：可自由停留的即時視聽作品；「療癒」描述體驗方向，不宣稱治療效果。
- 現有場景：水光之間、樹影午後、海光之室。未來可加入雨窗、街角、庭院等日常場景，具體場景仍待製作。
- 每個場景必須有不同的空間構圖與主要自然現象；不以換色或增加裝飾湊場景數。
- 第一版不需要帳號、資料庫、付費系統、CMS、AI 即時生成服務或分享社群；用版本化檔案管理場景。

### 品牌命名（2026-09-07）

- 對外使用 **Quiet Places／靜隅**，英文頁首可使用 `QUIET PLACES`；套件、repository 和新工具命名空間使用 `quiet-places`。
- 品牌短句：「在日常的一隅，停留片刻。」集合可承載多種日常場景，不以水景限定整個產品。
- SVG 圖標位於 `public/favicon.svg`，以天窗光線與平靜水線代表從水光出發的安靜角落；64×64 viewBox，可縮放，網站 favicon 和 README 共用同一份資產。
- 偏好儲存改用 `quiet-places.preferences`，相容讀取舊 key，遷移寫入失敗仍保留已讀取的使用者設定。
- 舊工作日誌與部署紀錄中的名稱、實際路徑保留為歷史證據；不改寫 Git 歷史、既有 Blender 二進位資產或其他仍在使用的 checkout 目錄。

## 2. 資訊架構與使用路徑

| 入口 | 使用者看到什麼 | 行為 |
|---|---|---|
| `#/` 集合首頁 | 品牌、短句、大幅靜態場景預覽、名稱 | 選擇一個已可用的場景 |
| `#/place/waterlight` | 水光之間全螢幕空間 | 停留、拖曳、調光、暫停、選用環境聲 |
| 場景內「換個地方」 | 輕量場景選擇層 | 選擇下一處或回首頁；關閉則回原場景 |
| 場景內「調整空間」 | 共通控制＋目前場景獨有控制 | 調整後立即看到結果，不切離畫面 |
| 無法載入／不支援 WebGL | 說明與靜態預覽（若已有） | 重試或回集合；不默認成黑畫面 |

採 hash 路由以讓普通靜態主機也能直接開啟場景連結；這是待實作的決策，不是現有網址功能。

首次進入集合讓使用者自行選擇。直接開啟場景連結則載入指定場景。瀏覽器返回／前進對應場景與首頁；未知 ID 顯示「找不到這個地方」並提供回首頁，不偷偷開另一個場景。

## 3. 介面與互動原則

- 首頁像作品集：少量大圖與留白，不做指標卡片、分類儀表板或推薦排行。三個場景不需要搜尋。
- 首頁預覽用靜態圖片，不同時啟動多個 WebGL canvas；預覽不得用概念圖冒充已完成畫面。
- 進入場景後 UI 淡出；移動、觸碰、Tab 能喚回。焦點在控制項時不隱藏；面板開啟時不自動消失。
- 共通操作：換個地方、暫停、聲音開關／音量、全螢幕。場景獨有控制由場景描述宣告，只顯示該場景支持的項目。
- 水光之間保留已接受的 90° 環繞與明顯耶穌光，不為集合視覺一致性而削弱它。雨窗可以完全固定鏡頭。
- 音訊預設關閉，第一次啟動需手勢；換場景延續本次聲音意願，但重新開啟頁面不自動播放。
- 暫停只凍結自然現象；使用者仍可操作鏡頭與設定。reduced-motion 初始暫停，淡入淡出改為立即切換。
- 不加連續使用天數、打卡、倒數結束、勝負、通知或自動換景。停留時間由使用者決定。

## 4. 設定語意

| 範圍 | 設定 | 保存策略（待實作） |
|---|---|---|
| 共通 | 音量、畫質偏好 | 有版本的 localStorage；讀取失敗使用預設 |
| 本次工作階段 | 是否播放聲音、暫停 | 記憶體，不跨頁面自動開聲 |
| 場景 | 手動時刻、跟隨本地時間、光束／雨量等 | 以場景 ID 分開保存；提供該場景恢復預設 |
| 鏡頭 | 拖曳位置 | 進入時回作者構圖，不持久保存 |

首版不自動偵測地點或真實天氣。本地時間是裝置時鐘；各場景自己解釋時刻對氣氛的影響，沒有一個通用「魚速」或「光束」設定。

## 5. 交付順序與驗收

| 階段 | 工作 | 完成條件 |
|---|---|---|
| P0 文件（本次） | 集合規格、場景目錄、架構與遷移順序 | 現況／規劃分開，原場景規格保留，文件連結有效 |
| P1 抽出播放器與 Waterlight adapter | 單 renderer／RAF、完整 dispose、場景控制契約 | 仍先直接開水光之間；與 `b0e28cd` 的接受畫面及控制比較，不能先改藝術方向 |
| P2 集合入口 | 靜態預覽、hash 路由、選擇層、設定保存 | 只列已可用場景；首頁↔水光反覆切換、重試／返回鍵／鍵盤可用 |
| P3 第二場景：雨停以前 | 不同的玻璃雨滴動態與固定鏡頭 | 沒有水光專用控制殘留；兩場景切換資源回收與音訊中斷通過，完成第二次使用者視覺確認 |
| P4 第三場景：樹影午後 | 柔和葉影與風的節奏 | 能明顯區別於水光與雨窗；三場景集合完成實機驗收 |
| P5 公開發布 | 最終名稱、授權清單、公開 repo／靜態部署 | GitHub 公開與網站部署分別驗證，不能以建置成功代替 |

P1～P5 是未完成工作，不是本次已交付功能。先用第二場景檢驗抽象，再決定要不要增加通用 shader／模組；避免先建一套龐大引擎。

## 6. 集合版共同驗收（規劃）

- [ ] 首頁與各場景路由、返回／前進、未知 ID 均符合上述語意。
- [ ] 桌面 1280×720、手機尺寸 390×844、窄矮視窗 375×667 可操作；另以實體手機量測。
- [ ] 連續 20 次切換後只有一個活躍 RAF、一個 renderer／AudioContext，上一場景沒有殘留聲音或事件。
- [ ] 量測暖機後資源計數不隨切換次數持續成長；以釋放證據判斷，不要求瀏覽器記憶體立刻歸零。
- [ ] 在選定測試裝置以桌面目標 60 fps、手機低品質目標 30 fps 記錄幀時間；這些是目標，尚未量測。
- [ ] 首次音訊手勢、靜音、背景分頁、減少動態、快速連點換景、載入失敗、WebGL context lost 可復原。
- [ ] 每個場景完成 30 秒安靜觀看及支持鏡頭範圍的人工驗收，不只跑 build。

## 文件入口

- [技術架構與遷移](docs/ARCHITECTURE.md)
- [場景目錄與創作規格](docs/SCENES.md)
- [水光之間詳細規格](docs/scenes/waterlight.md)
- [既有本機驗收紀錄](docs/VALIDATION.md)
- [原始創作描述](docs/original-brief.txt)
